import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    backgroundColor: "#1F1E25",
    borderRadios: 5,
    flexDirection: "row",
    alignItems: "center",
  },

  name: {
    flex: 1,
    color: "#FFFF",
    fontSize: 16,
    padding: 16
  },
});
